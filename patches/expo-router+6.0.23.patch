diff --git a/node_modules/expo-router/_ctx-shared.js b/node_modules/expo-router/_ctx-shared.js
index d8e4374..45c1b7a 100644
--- a/node_modules/expo-router/_ctx-shared.js
+++ b/node_modules/expo-router/_ctx-shared.js
@@ -1,5 +1,7 @@
-// Ignore root `./+html.js` and API route files `./generate+api.tsx`.
+// Patched version of expo-router/_ctx-shared.js
+// Adds (?:.*\.test) and (?:.*\.spec) to the exclusion list so that
+// co-located test files in src/app/ are not treated as routes.
 module.exports = {
   EXPO_ROUTER_CTX_IGNORE:
-    /^(?:\.\/)(?!(?:(?:(?:.*\+api)|(?:\+(html|native-intent))))\.[tj]sx?$).*\.[tj]sx?$/,
+    /^(?:\.\/)(?!(?:(?:(?:.*\+api)|(?:\+(html|native-intent))|(?:.*\.test)|(?:.*\.spec)))\.[tj]sx?$).*\.[tj]sx?$/,
 };
diff --git a/node_modules/expo-router/_ctx.android.js b/node_modules/expo-router/_ctx.android.js
index 1afb1f8..db34626 100644
--- a/node_modules/expo-router/_ctx.android.js
+++ b/node_modules/expo-router/_ctx.android.js
@@ -1,6 +1,10 @@
+// Patched version of expo-router/_ctx.android.js
+// Adds (?:.*\.test) and (?:.*\.spec) to the exclusion list so that
+// co-located test files in src/app/ are not treated as routes.
 export const ctx = require.context(
   process.env.EXPO_ROUTER_APP_ROOT,
   true,
-  /^(?:\.\/)(?!(?:(?:(?:.*\+api)|(?:\+html)|(?:\+middleware)))\.[tj]sx?$).*(?:\.ios|\.web)?\.[tj]sx?$/,
+  // Ignore +html, +middleware, API routes, and test/spec files
+  /^(?:\.\/)(?!(?:(?:(?:.*\+api)|(?:\+html)|(?:\+middleware)|(?:.*\.test)|(?:.*\.spec)))\.[tj]sx?$).*(?:\.ios|\.web)?\.[tj]sx?$/,
   process.env.EXPO_ROUTER_IMPORT_MODE
 );
diff --git a/node_modules/expo-router/_ctx.ios.js b/node_modules/expo-router/_ctx.ios.js
index b30ee6c..8dfabd9 100644
--- a/node_modules/expo-router/_ctx.ios.js
+++ b/node_modules/expo-router/_ctx.ios.js
@@ -1,6 +1,10 @@
+// Patched version of expo-router/_ctx.ios.js
+// Adds (?:.*\.test) and (?:.*\.spec) to the exclusion list so that
+// co-located test files in src/app/ are not treated as routes.
 export const ctx = require.context(
   process.env.EXPO_ROUTER_APP_ROOT,
   true,
-  /^(?:\.\/)(?!(?:(?:(?:.*\+api)|(?:\+html)|(?:\+middleware)))\.[tj]sx?$).*(?:\.android|\.web)?\.[tj]sx?$/,
+  // Ignore +html, +middleware, API routes, and test/spec files
+  /^(?:\.\/)(?!(?:(?:(?:.*\+api)|(?:\+html)|(?:\+middleware)|(?:.*\.test)|(?:.*\.spec)))\.[tj]sx?$).*(?:\.android|\.web)?\.[tj]sx?$/,
   process.env.EXPO_ROUTER_IMPORT_MODE
 );
diff --git a/node_modules/expo-router/_ctx.js b/node_modules/expo-router/_ctx.js
index 0979e39..6d44afa 100644
--- a/node_modules/expo-router/_ctx.js
+++ b/node_modules/expo-router/_ctx.js
@@ -1,6 +1,9 @@
+// Patched version of expo-router/_ctx.js
+// Adds (?:.*\.test) and (?:.*\.spec) to the exclusion list so that
+// co-located test files in src/app/ are not treated as routes.
 export const ctx = require.context(
   process.env.EXPO_ROUTER_APP_ROOT,
   true,
-  // Ignore root `./+html.js` and API route files `./generate+api.tsx`.
-  /^(?:\.\/)(?!(?:(?:(?:.*\+api)|(?:\+html)))\.[tj]sx?$).*\.[tj]sx?$/
+  // Ignore +html, API routes, and test/spec files
+  /^(?:\.\/)(?!(?:(?:(?:.*\+api)|(?:\+html)|(?:.*\.test)|(?:.*\.spec)))\.[tj]sx?$).*\.[tj]sx?$/
 );
diff --git a/node_modules/expo-router/_ctx.web.js b/node_modules/expo-router/_ctx.web.js
index 8061699..9ec2753 100644
--- a/node_modules/expo-router/_ctx.web.js
+++ b/node_modules/expo-router/_ctx.web.js
@@ -1,6 +1,10 @@
+// Patched version of expo-router/_ctx.web.js
+// Adds (?:.*\.test) and (?:.*\.spec) to the exclusion list so that
+// co-located test files in src/app/ are not treated as routes.
 export const ctx = require.context(
   process.env.EXPO_ROUTER_APP_ROOT,
   true,
-  /^(?:\.\/)(?!(?:(?:(?:.*\+api)|(?:\+middleware)|(?:\+(html|native-intent))))\.[tj]sx?$).*(?:\.android|\.ios|\.native)?\.[tj]sx?$/,
+  // Ignore +html, +middleware, API routes, native-intent, and test/spec files
+  /^(?:\.\/)(?!(?:(?:(?:.*\+api)|(?:\+middleware)|(?:\+(html|native-intent))|(?:.*\.test)|(?:.*\.spec)))\.[tj]sx?$).*(?:\.android|\.ios|\.native)?\.[tj]sx?$/,
   process.env.EXPO_ROUTER_IMPORT_MODE
 );
diff --git a/node_modules/expo-router/build/routes-manifest.js b/node_modules/expo-router/build/routes-manifest.js
index 687bd53..97f7cb8 100644
--- a/node_modules/expo-router/build/routes-manifest.js
+++ b/node_modules/expo-router/build/routes-manifest.js
@@ -13,8 +13,11 @@ function createMockContextModule(map = []) {
     return contextModule;
 }
 function createRoutesManifest(paths, options) {
+    // Filter out co-located test/spec files so they are not treated as routes.
+    // The glob in @expo/cli router.js includes ALL *.ts/tsx files with no filter.
+    const filteredPaths = paths.filter((p) => !/\.(test|spec)\.[jt]sx?$/.test(p));
     // TODO: Drop this part for Node.js
-    const routeTree = (0, getRoutesSSR_1.getRoutes)(createMockContextModule(paths), {
+    const routeTree = (0, getRoutesSSR_1.getRoutes)(createMockContextModule(filteredPaths), {
         ...options,
         preserveApiRoutes: true,
         preserveRedirectAndRewrites: true,
